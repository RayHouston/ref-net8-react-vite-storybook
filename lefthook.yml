pre-commit:
    parallel: true
    commands:
        lint-components:
            run: cd web.client.components && npx lint-staged
        lint-client:
            run: cd web.client && npx lint-staged
